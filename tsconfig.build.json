/** emitted typescript files that are in final distribution */
/*
 * Emitted:
 * - JS : true
 * - Types : true
 */
{
    "extends": "@snailicid3/config/tsconfig.library",
    "include": ["./src/**/*.ts"],
    "exclude": ["**/*.test.ts"],
    "compilerOptions": {
        "types": ["node"],
        "outDir": "./types"
    }
}
