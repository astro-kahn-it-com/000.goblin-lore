diff --git a/node_modules/@snailicid3/config/bin/changeset-branch.sh b/node_modules/@snailicid3/config/bin/changeset-branch.sh
index 47c7ff4..4e3b30a 100755
--- a/node_modules/@snailicid3/config/bin/changeset-branch.sh
+++ b/node_modules/@snailicid3/config/bin/changeset-branch.sh
@@ -3,6 +3,14 @@ set -euo pipefail
 
 # START SH BOOTSTRAP LOADER
 SCRIPT_SOURCE_PATH="${BASH_SOURCE[0]}"
+if [[ -L "$SCRIPT_SOURCE_PATH" ]]; then
+    SCRIPT_SOURCE_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
+    SCRIPT_LINK_TARGET="$(readlink "$SCRIPT_SOURCE_PATH")"
+    case "$SCRIPT_LINK_TARGET" in
+        /*) SCRIPT_SOURCE_PATH="$SCRIPT_LINK_TARGET" ;;
+        *) SCRIPT_SOURCE_PATH="$SCRIPT_SOURCE_DIR/$SCRIPT_LINK_TARGET" ;;
+    esac
+fi
 LOADER_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
 
 resolve_bootstrap_path() {
@@ -34,6 +42,15 @@ unset BOOTSTRAP_CALLER_SOURCE
 # END SH BOOTSTRAP LOADER
 PREFIX="${PREFIX_OVERRIDE:-${PREFIX:-changeset}}"
 ALLOW_DIRTY="${ALLOW_DIRTY:-false}"
+PACKAGE_MANAGER="${PACKAGE_MANAGER:-npm}"
+
+package_exec() {
+    case "$PACKAGE_MANAGER" in
+        npm) npm exec -- "$@" ;;
+        pnpm) pnpm exec "$@" ;;
+        *) die "unsupported PACKAGE_MANAGER: $PACKAGE_MANAGER" ;;
+    esac
+}
 
 detect_default_branch() {
     # Prefer origin/HEAD if available
@@ -91,7 +108,7 @@ find .changeset -maxdepth 1 -type f -name '*.md' -print | sort > "$before"
 spacer 1
 section "Launching Changesets CLI..."
 spacer 1
-pnpm changeset add
+package_exec changeset add
 
 # Find the new file
 find .changeset -maxdepth 1 -type f -name '*.md' -print | sort > "$after"
@@ -106,7 +123,7 @@ new_count="$(comm -13 "$before" "$after" | wc -l | tr -d ' ')"
 
 slug="$(basename "$new_file" .md)"
 branch="${PREFIX}/${slug}"
-scope="$(pnpm exec scope-affected --changeset-only "$new_file")"
+scope="$(package_exec scope-affected --changeset-only "$new_file")"
 
 kv_pair "New changeset" "$new_file"
 kv_pair "Commit scope" "$scope"
@@ -122,7 +139,7 @@ git switch -c "$branch"
 log "Committing changeset..."
 spacer 1
 git add "$new_file"
-pnpm exec scope-commit --checked-commit changeset "$slug" --scope "$scope"
+package_exec scope-commit --checked-commit changeset "$slug" --scope "$scope"
 
 success "Done."
 kv_pair "Branch" "$branch"
diff --git a/node_modules/@snailicid3/config/bin/completions/install-pnpm.sh b/node_modules/@snailicid3/config/bin/completions/install-pnpm.sh
index 080c34c..985defb 100755
--- a/node_modules/@snailicid3/config/bin/completions/install-pnpm.sh
+++ b/node_modules/@snailicid3/config/bin/completions/install-pnpm.sh
@@ -3,6 +3,14 @@ set -euo pipefail
 
 # START SH BOOTSTRAP LOADER
 SCRIPT_SOURCE_PATH="${BASH_SOURCE[0]}"
+if [[ -L "$SCRIPT_SOURCE_PATH" ]]; then
+    SCRIPT_SOURCE_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
+    SCRIPT_LINK_TARGET="$(readlink "$SCRIPT_SOURCE_PATH")"
+    case "$SCRIPT_LINK_TARGET" in
+        /*) SCRIPT_SOURCE_PATH="$SCRIPT_LINK_TARGET" ;;
+        *) SCRIPT_SOURCE_PATH="$SCRIPT_SOURCE_DIR/$SCRIPT_LINK_TARGET" ;;
+    esac
+fi
 LOADER_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
 
 resolve_bootstrap_path() {
diff --git a/node_modules/@snailicid3/config/bin/esbuild-patch/patch.sh b/node_modules/@snailicid3/config/bin/esbuild-patch/patch.sh
index eb7223c..8d33bcf 100755
--- a/node_modules/@snailicid3/config/bin/esbuild-patch/patch.sh
+++ b/node_modules/@snailicid3/config/bin/esbuild-patch/patch.sh
@@ -1,9 +1,19 @@
 #!/usr/bin/env bash
 # Replace esbuild's Darwin x64 executable with a Catalina-compatible build.
 
-BOOTSTRAP_CALLER_SOURCE="${BASH_SOURCE[0]}"
+SCRIPT_SOURCE_PATH="${BASH_SOURCE[0]}"
+if [[ -L "$SCRIPT_SOURCE_PATH" ]]; then
+    SCRIPT_SOURCE_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
+    SCRIPT_LINK_TARGET="$(readlink "$SCRIPT_SOURCE_PATH")"
+    case "$SCRIPT_LINK_TARGET" in
+        /*) SCRIPT_SOURCE_PATH="$SCRIPT_LINK_TARGET" ;;
+        *) SCRIPT_SOURCE_PATH="$SCRIPT_SOURCE_DIR/$SCRIPT_LINK_TARGET" ;;
+    esac
+fi
+
+BOOTSTRAP_CALLER_SOURCE="$SCRIPT_SOURCE_PATH"
 # shellcheck source=../bootstrap.sh
-. "$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)/bootstrap.sh"
+. "$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")/.." && pwd)/bootstrap.sh"
 
 PATCH_BINARY="$SCRIPT_DIR/esbuild-darwin-x64"
 CONSUMER_DIR="${GBT_PATCH_CWD:-$PWD}"
diff --git a/node_modules/@snailicid3/config/bin/inspect-dependencies.sh b/node_modules/@snailicid3/config/bin/inspect-dependencies.sh
index de53e80..4fc77a1 100755
--- a/node_modules/@snailicid3/config/bin/inspect-dependencies.sh
+++ b/node_modules/@snailicid3/config/bin/inspect-dependencies.sh
@@ -3,6 +3,14 @@ set -euo pipefail
 
 # START SH BOOTSTRAP LOADER
 SCRIPT_SOURCE_PATH="${BASH_SOURCE[0]}"
+if [[ -L "$SCRIPT_SOURCE_PATH" ]]; then
+    SCRIPT_SOURCE_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
+    SCRIPT_LINK_TARGET="$(readlink "$SCRIPT_SOURCE_PATH")"
+    case "$SCRIPT_LINK_TARGET" in
+        /*) SCRIPT_SOURCE_PATH="$SCRIPT_LINK_TARGET" ;;
+        *) SCRIPT_SOURCE_PATH="$SCRIPT_SOURCE_DIR/$SCRIPT_LINK_TARGET" ;;
+    esac
+fi
 LOADER_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
 
 resolve_bootstrap_path() {
diff --git a/node_modules/@snailicid3/config/bin/make-executable.sh b/node_modules/@snailicid3/config/bin/make-executable.sh
index 7694aa8..3e24a89 100755
--- a/node_modules/@snailicid3/config/bin/make-executable.sh
+++ b/node_modules/@snailicid3/config/bin/make-executable.sh
@@ -5,6 +5,14 @@ set -euo pipefail
 
 # START SH BOOTSTRAP LOADER
 SCRIPT_SOURCE_PATH="${BASH_SOURCE[0]}"
+if [[ -L "$SCRIPT_SOURCE_PATH" ]]; then
+    SCRIPT_SOURCE_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
+    SCRIPT_LINK_TARGET="$(readlink "$SCRIPT_SOURCE_PATH")"
+    case "$SCRIPT_LINK_TARGET" in
+        /*) SCRIPT_SOURCE_PATH="$SCRIPT_LINK_TARGET" ;;
+        *) SCRIPT_SOURCE_PATH="$SCRIPT_SOURCE_DIR/$SCRIPT_LINK_TARGET" ;;
+    esac
+fi
 LOADER_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
 COMMAND_NAME="${COMMAND_NAME:-gbt-exec}"
 resolve_bootstrap_path() {
diff --git a/node_modules/@snailicid3/config/bin/scope/affected.sh b/node_modules/@snailicid3/config/bin/scope/affected.sh
index de60fb7..cbd4cb1 100755
--- a/node_modules/@snailicid3/config/bin/scope/affected.sh
+++ b/node_modules/@snailicid3/config/bin/scope/affected.sh
@@ -3,6 +3,14 @@ set -euo pipefail
 
 # START SH BOOTSTRAP LOADER
 SCRIPT_SOURCE_PATH="${BASH_SOURCE[0]}"
+if [[ -L "$SCRIPT_SOURCE_PATH" ]]; then
+    SCRIPT_SOURCE_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
+    SCRIPT_LINK_TARGET="$(readlink "$SCRIPT_SOURCE_PATH")"
+    case "$SCRIPT_LINK_TARGET" in
+        /*) SCRIPT_SOURCE_PATH="$SCRIPT_LINK_TARGET" ;;
+        *) SCRIPT_SOURCE_PATH="$SCRIPT_SOURCE_DIR/$SCRIPT_LINK_TARGET" ;;
+    esac
+fi
 LOADER_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
 
 resolve_bootstrap_path() {
diff --git a/node_modules/@snailicid3/config/bin/scope/commit.sh b/node_modules/@snailicid3/config/bin/scope/commit.sh
index 24226a0..d648aef 100755
--- a/node_modules/@snailicid3/config/bin/scope/commit.sh
+++ b/node_modules/@snailicid3/config/bin/scope/commit.sh
@@ -3,6 +3,14 @@ set -euo pipefail
 
 # START SH BOOTSTRAP LOADER
 SCRIPT_SOURCE_PATH="${BASH_SOURCE[0]}"
+if [[ -L "$SCRIPT_SOURCE_PATH" ]]; then
+    SCRIPT_SOURCE_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
+    SCRIPT_LINK_TARGET="$(readlink "$SCRIPT_SOURCE_PATH")"
+    case "$SCRIPT_LINK_TARGET" in
+        /*) SCRIPT_SOURCE_PATH="$SCRIPT_LINK_TARGET" ;;
+        *) SCRIPT_SOURCE_PATH="$SCRIPT_SOURCE_DIR/$SCRIPT_LINK_TARGET" ;;
+    esac
+fi
 LOADER_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
 
 resolve_bootstrap_path() {
diff --git a/node_modules/@snailicid3/config/bin/snail-sh-test.sh b/node_modules/@snailicid3/config/bin/snail-sh-test.sh
index ccd1537..abb7c30 100644
--- a/node_modules/@snailicid3/config/bin/snail-sh-test.sh
+++ b/node_modules/@snailicid3/config/bin/snail-sh-test.sh
@@ -3,6 +3,14 @@ set -euo pipefail
 
 # START SH BOOTSTRAP LOADER
 SCRIPT_SOURCE_PATH="${BASH_SOURCE[0]}"
+if [[ -L "$SCRIPT_SOURCE_PATH" ]]; then
+    SCRIPT_SOURCE_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
+    SCRIPT_LINK_TARGET="$(readlink "$SCRIPT_SOURCE_PATH")"
+    case "$SCRIPT_LINK_TARGET" in
+        /*) SCRIPT_SOURCE_PATH="$SCRIPT_LINK_TARGET" ;;
+        *) SCRIPT_SOURCE_PATH="$SCRIPT_SOURCE_DIR/$SCRIPT_LINK_TARGET" ;;
+    esac
+fi
 LOADER_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
 
 resolve_bootstrap_path() {
diff --git a/node_modules/@snailicid3/config/bin/uninstall.sh b/node_modules/@snailicid3/config/bin/uninstall.sh
index 034a82d..d318884 100755
--- a/node_modules/@snailicid3/config/bin/uninstall.sh
+++ b/node_modules/@snailicid3/config/bin/uninstall.sh
@@ -3,6 +3,14 @@ set -euo pipefail
 
 # START SH BOOTSTRAP LOADER
 SCRIPT_SOURCE_PATH="${BASH_SOURCE[0]}"
+if [[ -L "$SCRIPT_SOURCE_PATH" ]]; then
+    SCRIPT_SOURCE_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
+    SCRIPT_LINK_TARGET="$(readlink "$SCRIPT_SOURCE_PATH")"
+    case "$SCRIPT_LINK_TARGET" in
+        /*) SCRIPT_SOURCE_PATH="$SCRIPT_LINK_TARGET" ;;
+        *) SCRIPT_SOURCE_PATH="$SCRIPT_SOURCE_DIR/$SCRIPT_LINK_TARGET" ;;
+    esac
+fi
 LOADER_DIR="$(CDPATH= cd -- "$(dirname -- "$SCRIPT_SOURCE_PATH")" && pwd)"
 
 resolve_bootstrap_path() {
diff --git a/node_modules/@snailicid3/config/dist/lint-staged/api-functions.js b/node_modules/@snailicid3/config/dist/lint-staged/api-functions.js
index 6fd779b..e49eba7 100644
--- a/node_modules/@snailicid3/config/dist/lint-staged/api-functions.js
+++ b/node_modules/@snailicid3/config/dist/lint-staged/api-functions.js
@@ -1,8 +1,8 @@
 import { getLintStagedDefaultConfig } from './base.js';
 import { defineConfig, defineConfigBuilder, } from '../core/index.js';
 export const defineLintStagedConfig = (config) => defineConfig(config);
-export const buildFunctionLintStaged = defineConfigBuilder(({ overrides = {} }) => {
-    const config = getLintStagedDefaultConfig();
+export const buildFunctionLintStaged = defineConfigBuilder(({ packageManager = 'pnpm', overrides = {} }) => {
+    const config = getLintStagedDefaultConfig({ packageManager });
     const finalConfig = Object.assign({}, config, overrides);
     return defineLintStagedConfig(finalConfig);
 });
diff --git a/node_modules/@snailicid3/config/dist/lint-staged/base.js b/node_modules/@snailicid3/config/dist/lint-staged/base.js
index 5f6d6b4..49abd3e 100644
--- a/node_modules/@snailicid3/config/dist/lint-staged/base.js
+++ b/node_modules/@snailicid3/config/dist/lint-staged/base.js
@@ -1,6 +1,8 @@
 import { filterFileArrByGlob, JSLIKE_FILE_EXTENSIONS, PRETTIER_FILE_EXTENSIONS, } from './../shared.js';
 import { extensionsToGlob, toFileArgs } from './helpers.js';
-export const getLintStagedDefaultConfig = () => {
+const packageExec = (packageManager) => packageManager === 'npm' ? 'npm exec --' : 'pnpm exec';
+export const getLintStagedDefaultConfig = ({ packageManager = 'pnpm' } = {}) => {
+    const exec = packageExec(packageManager);
     const config = {
         '*.md': (staged) => {
             const filtered = filterFileArrByGlob(staged, ['**/*.api.md'], true);
@@ -8,20 +10,20 @@ export const getLintStagedDefaultConfig = () => {
             if (files.length === 0)
                 return [];
             return [
-                `pnpm exec prettier --write ${files.join(' ')}`,
-                `pnpm exec markdownlint-cli2 --no-globs --fix ${files.join(' ')} || true`,
+                `${exec} prettier --write ${files.join(' ')}`,
+                `${exec} markdownlint-cli2 --no-globs --fix ${files.join(' ')} || true`,
             ];
         },
-        '.gitignore': 'pnpm exec prettier --write .gitignore',
+        '.gitignore': `${exec} prettier --write .gitignore`,
         '.husky/**/*': (staged) => {
             const files = toFileArgs(staged);
-            return `pnpm exec prettier --write ${files.join(' ')}`;
+            return `${exec} prettier --write ${files.join(' ')}`;
         },
         [extensionsToGlob(JSLIKE_FILE_EXTENSIONS)]: (staged) => {
             const files = toFileArgs(staged);
             return [
-                `pnpm exec prettier --write ${files.join(' ')}`,
-                `pnpm exec eslint --fix ${files.join(' ')}`,
+                `${exec} prettier --write ${files.join(' ')}`,
+                `${exec} eslint --fix ${files.join(' ')}`,
             ];
         },
         [extensionsToGlob(PRETTIER_FILE_EXTENSIONS)]: (staged) => {
@@ -29,7 +31,7 @@ export const getLintStagedDefaultConfig = () => {
             const files = toFileArgs(filtered);
             if (files.length === 0)
                 return [];
-            return `pnpm exec prettier --write ${files.join(' ')}`;
+            return `${exec} prettier --write ${files.join(' ')}`;
         },
     };
     return config;
diff --git a/node_modules/@snailicid3/config/dist/scope/affected.js b/node_modules/@snailicid3/config/dist/scope/affected.js
index c27716d..a72c5b8 100644
--- a/node_modules/@snailicid3/config/dist/scope/affected.js
+++ b/node_modules/@snailicid3/config/dist/scope/affected.js
@@ -55,7 +55,10 @@ function collectNxAffectedScopes(repoRoot, parsed) {
     ];
     if (parsed.nxHead)
         nxArgs.push('--head', parsed.nxHead);
-    const result = runCommand('pnpm', nxArgs, { cwd: repoRoot });
+    const packageManager = process.env.PACKAGE_MANAGER ?? 'npm';
+    const result = packageManager === 'npm'
+        ? runCommand('npm', ['exec', '--', ...nxArgs], { cwd: repoRoot })
+        : runCommand('pnpm', nxArgs, { cwd: repoRoot });
     if (result.status !== 0)
         return [];
     return splitNonEmptyLines(result.stdout).map((scope) => normalizeScopeName(scope, parsed.keepPrefix));
diff --git a/node_modules/@snailicid3/config/dist/scope/commit.js b/node_modules/@snailicid3/config/dist/scope/commit.js
index 1b8b328..816c25e 100644
--- a/node_modules/@snailicid3/config/dist/scope/commit.js
+++ b/node_modules/@snailicid3/config/dist/scope/commit.js
@@ -202,7 +202,7 @@ function runCheckedPrecommit(repoRoot, options) {
     if (options.skipLintStaged) {
         return;
     }
-    const lintResult = runCommand('pnpm', ['exec', 'lint-staged', '--debug', '--relative'], { cwd: repoRoot, stdio: 'inherit' });
+    const lintResult = runPackageBinary(repoRoot, 'lint-staged', ['--debug', '--relative'], { stdio: 'inherit' });
     if (lintResult.status !== 0) {
         throw new Error(lintResult.stderr || lintResult.stdout || 'lint-staged failed');
     }
@@ -235,7 +235,7 @@ function splitExplicitScope(scopeValue) {
 function validateCommitMessage(repoRoot, message) {
     if (process.env.SCOPE_COMMIT_SKIP_COMMITLINT === '1')
         return;
-    const result = runCommand('pnpm', ['exec', 'commitlint', '--cwd', repoRoot], {
+    const result = runPackageBinary(repoRoot, 'commitlint', ['--cwd', repoRoot], {
         cwd: repoRoot,
         input: `${message}\n`,
     });
@@ -250,6 +250,22 @@ function validateCommitMessage(repoRoot, message) {
             .join('\n'));
     }
 }
+function runPackageBinary(repoRoot, binary, args, options = {}) {
+    const packageManager = process.env.PACKAGE_MANAGER ?? 'npm';
+    if (packageManager === 'npm') {
+        return runCommand('npm', ['exec', '--', binary, ...args], {
+            cwd: repoRoot,
+            ...options,
+        });
+    }
+    if (packageManager === 'pnpm') {
+        return runCommand('pnpm', ['exec', binary, ...args], {
+            cwd: repoRoot,
+            ...options,
+        });
+    }
+    throw new Error(`Unsupported PACKAGE_MANAGER: ${packageManager}`);
+}
 export default main;
 /*
 Console.log({
diff --git a/node_modules/@snailicid3/config/types/lint-staged/api-functions.d.ts b/node_modules/@snailicid3/config/types/lint-staged/api-functions.d.ts
index 1aa3594..79963b7 100644
--- a/node_modules/@snailicid3/config/types/lint-staged/api-functions.d.ts
+++ b/node_modules/@snailicid3/config/types/lint-staged/api-functions.d.ts
@@ -2,12 +2,15 @@ import { type Configuration } from 'lint-staged';
 import { type ConfigFunctionOptions } from '../core/index.js';
 export type LintStagedConfig = Configuration;
 export type LintStagedConfigFunctionOptions = ConfigFunctionOptions<{
+    /** Package manager used to execute lint-staged tasks. */
+    packageManager?: 'npm' | 'pnpm';
     /** Merged on top of the default lint-staged config. */
     overrides?: LintStagedConfig;
 }>;
 export declare const defineLintStagedConfig: <const TConfig extends LintStagedConfig>(config: TConfig) => TConfig;
 export declare const buildFunctionLintStaged: import("../index.js").ConfigBuilder<Configuration, {
     cwd: import("../index.js").ConfigCwd;
+    packageManager?: "npm" | "pnpm" | undefined;
     overrides?: LintStagedConfig | undefined;
 }>;
 //# sourceMappingURL=api-functions.d.ts.map
diff --git a/node_modules/@snailicid3/config/types/lint-staged/base.d.ts b/node_modules/@snailicid3/config/types/lint-staged/base.d.ts
index 0974672..203833e 100644
--- a/node_modules/@snailicid3/config/types/lint-staged/base.d.ts
+++ b/node_modules/@snailicid3/config/types/lint-staged/base.d.ts
@@ -1,3 +1,6 @@
 import { type Configuration as LintStagedConfig } from 'lint-staged';
-export declare const getLintStagedDefaultConfig: () => LintStagedConfig;
+export type LintStagedPackageManager = 'npm' | 'pnpm';
+export declare const getLintStagedDefaultConfig: (options?: {
+    packageManager?: LintStagedPackageManager;
+}) => LintStagedConfig;
 //# sourceMappingURL=base.d.ts.map
diff --git a/node_modules/@snailicid3/config/types/lint-staged/index.d.ts b/node_modules/@snailicid3/config/types/lint-staged/index.d.ts
index 3532e16..e7f99d4 100644
--- a/node_modules/@snailicid3/config/types/lint-staged/index.d.ts
+++ b/node_modules/@snailicid3/config/types/lint-staged/index.d.ts
@@ -3,6 +3,7 @@ import { type ConfigTool } from '../core/index.js';
 export declare const LintStaged: {
     config: import("../index.js").ConfigBuilder<import("lint-staged").Configuration, {
         cwd: import("../index.js").ConfigCwd;
+        packageManager?: "npm" | "pnpm" | undefined;
         overrides?: LintStagedConfig | undefined;
     }>;
     defineConfig: <const TConfig extends LintStagedConfig>(config: TConfig) => TConfig;
