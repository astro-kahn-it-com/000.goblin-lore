# 🔧 TASK: 003.09.04 — Implement the Embedded-Emphasis Fidelity Test

**Task ID:** `TASK-003.09.04` **Parent Feature:** [FEAT-003.09] THE RESOLUTION CONDITION FIDELITY
TEST **Parent Epic:** [EPIC-003] THE ROUND-TRIP GUARANTEE **Governing Artifact:**
`docs/testing/round-trip-catalog.md` **Governing Policy:** [POLICY-01] ENGINE DETERMINISM & LEDGER
INTEGRITY

**Source Lineage**

- **Milestone Source:** `003.milestone-determinism-extraction-fidelity-gate.md`
- **Epic Source:** `003-EPIC-003-the-round-trip-guarantee.md`
- **Feature Source:** `003.09-FEAT-WS-003.09-the-resolution-condition-fidelity-test.md`

---

### **I. TASK DESCRIPTION**

**User Story** As a Backend Lead, I need to test that a Resolution Condition containing markdown
emphasis (like `*italic*` or `**bold**`) is extracted exactly as raw markdown, not accidentally
stripped or rendered by the extractor logic.

**Context & Value** Confirms raw text extraction fidelity.

- **The Strategic Value:** Ensures authors can use standard markdown in their conditions without
  data loss.
- **The Technical Challenge:** Validating regex handling of markdown asterisks.
- **The Architectural Constraints:** Unit test.
- **The Standard:** Raw markdown syntax preserved.

**Technical Constraints**

- **Target File:** `goblin-extract/test.ts`.

---

### **II. SUBTASKS**

#### **Phase 1: Implementation** — Est: 1h

- [ ] **[1.1] Build Embedded-Emphasis Test**

  ```typescript
  // Append to goblin-extract/test.ts
  it('extracts a Resolution Condition containing markdown emphasis exactly', () => {
    // 1. Setup fixture with: "**Resolution Condition:** They leave *immediately*."
    // 2. Extract
    // 3. Assert resolution_condition === 'They leave *immediately*.'
  });
  # PASS condition: vitest passes for the emphasis test.
  ```

---

### **III. ACCEPTANCE CRITERIA (Definition of Done)**

_Every checkbox must be ✅ before this task can be marked CLOSED. Partial completion is not
accepted._

- [ ] **AC-1 — Test Passes:** The extracted string includes the markdown asterisk tokens exactly as
      authored.

---

### **IV. RELATIONSHIPS (The Critical Path)**

**Blocking Downstream:**

| ID             | Name                   | Impact                       |
| -------------- | ---------------------- | ---------------------------- |
| TASK-003.09.05 | Full Boundary-Case Run | Execution requires all tests |

**Waiting On (Upstream Dependencies):**

| ID             | Name                                        | Required State                   |
| -------------- | ------------------------------------------- | -------------------------------- |
| TASK-003.09.01 | Implement the Short-Condition Fidelity Test | Base testing pattern established |

---

### **V. TASK METADATA**

| Attribute         | Value                                                |
| ----------------- | ---------------------------------------------------- |
| **Priority**      | 🟡 Medium                                            |
| **Status**        | ⚪ Backlog                                           |
| **Owner**         | Backend Lead                                         |
| **Time Estimate** | 1 hour _(Phase 1: 1h)_                               |
| **Sprint Points** | 1                                                    |
| **Tags**          | `testing`, `extraction`, `markdown`                  |
| **Epic**          | [EPIC-003] THE ROUND-TRIP GUARANTEE                  |
| **Feature**       | [FEAT-003.09] THE RESOLUTION CONDITION FIDELITY TEST |
| **Value**         | Syntax Preservation Check                            |
| **Component**     | `["Packages"]`                                       |
| **Risk Level**    | Low _(Unit test addition.)_                          |
| **Reporter**      | Technical Program Manager                            |

---

> **⚠️ ENFORCEMENT NOTICE:** If the regex or intermediate parsing strips the markdown syntax, it
> must be fixed to preserve raw input.
