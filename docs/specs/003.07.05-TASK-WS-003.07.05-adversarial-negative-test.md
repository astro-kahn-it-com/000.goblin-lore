# 🔧 TASK: 003.07.05 — Adversarial Negative Test

**Task ID:** `TASK-003.07.05` **Parent Feature:** [FEAT-003.07] THE FIELD-PRESERVATION TEST SUITE
**Parent Epic:** [EPIC-003] THE ROUND-TRIP GUARANTEE **Governing Artifact:**
`docs/testing/round-trip-catalog.md` **Governing Policy:** [POLICY-01] ENGINE DETERMINISM & LEDGER
INTEGRITY

**Source Lineage**

- **Milestone Source:** `003.milestone-determinism-extraction-fidelity-gate.md`
- **Epic Source:** `003-EPIC-003-the-round-trip-guarantee.md`
- **Feature Source:** `003.07-FEAT-WS-003.07-the-field-preservation-test-suite.md`

---

### **I. TASK DESCRIPTION**

**User Story** As a QA Lead, I need to deliberately introduce a field-leakage bug into `extract.ts`
(e.g. unconditionally appending a space to a location string) and confirm the round-trip matrix
runner catches it, then revert the bug.

**Context & Value** Proves the test suite actually guards against the bugs it claims to detect.

- **The Strategic Value:** Validates the validation mechanism itself (true positive testing).
- **The Technical Challenge:** N/A (Temporary manual injection).
- **The Architectural Constraints:** Must be a manual audit.
- **The Standard:** Test runner successfully fails.

**Technical Constraints**

- **Target File:** `goblin-extract/src/extract.ts`.

---

### **II. SUBTASKS**

#### **Phase 2: Validation** — Est: 3h

- [x] **[2.1] Validate Suite Sensitivity**

  ```bash
  # Developer instruction:
  # 1. Modify goblin-extract/src/extract.ts to leak an edit (e.g., location = location + " ").
  # 2. Run the round-trip test matrix.
  # 3. Confirm the test fails on an unexpected field mutation.
  # 4. Revert the modification.
  echo "Verify round-trip matrix catches field leakage"
  # PASS condition: Manual validation complete.
  ```

---

### **III. ACCEPTANCE CRITERIA (Definition of Done)**

_Every checkbox must be ✅ before this task can be marked CLOSED. Partial completion is not
accepted._

- [x] **AC-1 — Suite Fails Properly:** Injecting a bug causes the runner to exit Code 1 on
      unexpected field mutation.

---

### **IV. RELATIONSHIPS (The Critical Path)**

**Blocking Downstream:**

| ID          | Name                           | Impact                                 |
| ----------- | ------------------------------ | -------------------------------------- |
| FEAT-003.08 | The Relational Tag Merge Logic | Round-trip pattern validated for reuse |

**Waiting On (Upstream Dependencies):**

| ID             | Name                         | Required State                    |
| -------------- | ---------------------------- | --------------------------------- |
| TASK-003.07.04 | Run the Full Scenario Matrix | Baseline pass must be established |

---

### **V. TASK METADATA**

| Attribute         | Value                                           |
| ----------------- | ----------------------------------------------- |
| **Priority**      | 🔴 High                                         |
| **Status**        | 🟢 CLOSED                                       |
| **Owner**         | QA Lead                                         |
| **Time Estimate** | 3 hours _(Phase 2: 3h)_                         |
| **Sprint Points** | 1                                               |
| **Tags**          | `testing`, `adversarial`, `validation`          |
| **Epic**          | [EPIC-003] THE ROUND-TRIP GUARANTEE             |
| **Feature**       | [FEAT-003.07] THE FIELD-PRESERVATION TEST SUITE |
| **Value**         | Suite Integrity Validation                      |
| **Component**     | `["Packages"]`                                  |
| **Risk Level**    | Low _(Manual test-of-tests execution.)_         |
| **Reporter**      | Technical Program Manager                       |

---

> **⚠️ ENFORCEMENT NOTICE:** If the test suite passes despite the injected bug, the diffing logic is
> broken and must be fixed.
