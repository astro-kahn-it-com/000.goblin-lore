# 🔧 TASK: 003.08.04 — Disjoint Merge and Removal-Limitation Test

**Task ID:** `TASK-003.08.04` **Parent Feature:** [FEAT-003.08] THE RELATIONAL TAG MERGE LOGIC
**Parent Epic:** [EPIC-003] THE ROUND-TRIP GUARANTEE **Governing Artifact:**
`docs/testing/round-trip-catalog.md` **Governing Policy:** [POLICY-01] ENGINE DETERMINISM & LEDGER
INTEGRITY

**Source Lineage**

- **Milestone Source:** `003.milestone-determinism-extraction-fidelity-gate.md`
- **Epic Source:** `003-EPIC-003-the-round-trip-guarantee.md`
- **Feature Source:** `003.08-FEAT-WS-003.08-the-relational-tag-merge-logic.md`

---

### **I. TASK DESCRIPTION**

**User Story** As a Backend Lead, I need to create a test asserting that distinct seeded and
explicit IDs both appear in the merge. Further, I need to explicitly test and document that
attempting to "remove" an explicit tag by deleting it from the outline text fails to remove it from
`linked_grievances` (the additive-only limitation).

**Context & Value** Confirms union behavior and explicitly codifies a known limitation in the merge
strategy.

- **The Strategic Value:** Documents the limitation that tags cannot be removed via hybrid authoring
  yet.
- **The Technical Challenge:** Structuring a test that intentionally passes on an incomplete feature
  (tag removal).
- **The Architectural Constraints:** Must include inline documentation and update
  `round-trip-catalog.md`.
- **The Standard:** Confirmed union behavior and documented limitation.

**Technical Constraints**

- **Target File:** `goblin-extract/test.ts` and `docs/testing/round-trip-catalog.md`.

---

### **II. SUBTASKS**

#### **Phase 1: Validation & Audit** — Est: 3h

- [ ] **[1.1] Build Disjoint Merge and Limitation Test**

  ```typescript
  // Append to goblin-extract/test.ts
  it('merges disjoint tags and KNOWN LIMITATION: ignores removal attempts', () => {
    // 1. Setup fixture with { linked_grievances: ['GR-123'] }
    // 2. Ensure body text HAS [[GR-456]]
    // 3. Extract and assert linked_grievances includes BOTH (Disjoint merge)

    // 4. Second extraction: simulate author removing [[GR-456]] from text
    // 5. Extract and assert linked_grievances STILL includes BOTH (Limitation)
  });
  # PASS condition: vitest passes for the disjoint merge test.
  ```

- [ ] **[1.2] Document Limitation in Catalog**

  ```bash
  cat << 'EOF' >> worker-sower/docs/testing/round-trip-catalog.md
  
  ### Relational Merge Limitation
  The `linked_grievances` merge logic is strictly additive. An author cannot remove a seeded grievance link by simply omitting its tag from the markdown body. This is a known, tested limitation favoring data safety over authoring flexibility.
  EOF
  # PASS condition: grep -q "Relational Merge Limitation" worker-sower/docs/testing/round-trip-catalog.md
  ```

---

### **III. ACCEPTANCE CRITERIA (Definition of Done)**

_Every checkbox must be ✅ before this task can be marked CLOSED. Partial completion is not
accepted._

- [ ] **AC-1 — Test Passes:** The disjoint union succeeds, and the removal-attempt test confirms the
      tag persists.
- [ ] **AC-2 — Catalog Updated:** The limitation is documented in `round-trip-catalog.md`.

---

### **IV. RELATIONSHIPS (The Critical Path)**

**Blocking Downstream:**

| ID          | Name                                   | Impact             |
| ----------- | -------------------------------------- | ------------------ |
| FEAT-003.09 | The Resolution Condition Fidelity Test | Completes Epic 003 |

**Waiting On (Upstream Dependencies):**

| ID             | Name                   | Required State                   |
| -------------- | ---------------------- | -------------------------------- |
| TASK-003.08.03 | Overlapping Merge Test | All simpler merge cases complete |

---

### **V. TASK METADATA**

| Attribute         | Value                                         |
| ----------------- | --------------------------------------------- |
| **Priority**      | 🟢 Low                                        |
| **Status**        | ⚪ Backlog                                    |
| **Owner**         | Backend Lead                                  |
| **Time Estimate** | 3 hours _(Phase 1: 3h)_                       |
| **Sprint Points** | 1                                             |
| **Tags**          | `testing`, `merge`, `limitations`             |
| **Epic**          | [EPIC-003] THE ROUND-TRIP GUARANTEE           |
| **Feature**       | [FEAT-003.08] THE RELATIONAL TAG MERGE LOGIC  |
| **Value**         | Formal Limitation Documentation               |
| **Component**     | `["Packages", "Documentation"]`               |
| **Risk Level**    | Low _(Unit test and documentation addition.)_ |
| **Reporter**      | Technical Program Manager                     |

---

> **⚠️ ENFORCEMENT NOTICE:** The test must explicitly document the removal limitation in its name
> and assertions.
