export async function testDialogueEdit(fixturePath: string) {
    // 1. Read fixture, mutate dialogue
    // 2. Extract
    // 3. Diff against baseline
    // 4. Assert only dialogue diverged, everything else identical
}
