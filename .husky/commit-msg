#!/usr/bin/env bash
set -eu

REPO_ROOT="$(git rev-parse --show-toplevel)"
. "$REPO_ROOT/bin/git-hooks/functions.sh"

commit_message_file="$1"
run_package_binary commitlint --edit "$commit_message_file"
